#include <stdio.h>

/**
* main - Entry point
* @argc: the number of parametrs
* @av: the sirng parametr
* Return: Always 0 (Success)
*/

int	main(int argc, char **av)
{
	printf("%i\n", argc - 1);
	return (0);
}
