#include "main.h"
#include <stdio.h>
/**
 * main - print programe's name
 * @argc: size of the array
 * @argv: the array passed
 * Return: always 0
 */
int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
